/**
 * Single place for public-loan / signup fallback partner code when admin config is unset.
 * Keep in sync with `frontend-master/src/config/publicReferral.js`.
 */
export const PUBLIC_LOAN_REFERRAL_FALLBACK_PARTNER_CODE = "PT-D4CTD8B2";

/** Client path for partner self-registration (same as frontend `PARTNER_REGISTRATION_ROUTE` without leading slash). */
export const PARTNER_REGISTRATION_PATH_SEGMENT = "PartnerRegistrationForm";
